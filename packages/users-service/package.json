{
  "name": "@kotletti/users-service",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf ./build",
    "build": "tsc -b",
    "rebuild": "npm run clean && npm run build",
    "start": "node ./build",
    "watch": "nodemon --config nodemon.json ./src/index.ts",
    "prettier:check": "prettier --check ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
